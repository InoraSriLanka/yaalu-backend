const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function check() { const u = await prisma.user.findFirst({ where: { email: { contains: 'testuser178945030' } }, include: { customerProfile: true } }); console.log('--- PostgreSQL DB Record for Newly Registered User ---'); console.log(JSON.stringify(u, null, 2)); await prisma.$disconnect(); } check();
